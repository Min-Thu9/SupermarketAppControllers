<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>
<style>
  .hero {
    position: relative;
    text-align: center;
    padding: 100px 20px;
    color: white;
    border-radius: 15px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    margin-top: 30px;
    background: 
      linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)),
      url('https://plus.unsplash.com/premium_photo-1664305032567-2c460e29dec1?ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MXx8c3VwZXJtYXJrZXR8ZW58MHx8MHx8fDA%3D&auto=format&fit=crop&q=60&w=600')
      no-repeat center center/cover;
  }

  .hero h2, .hero p {
    text-shadow: 1px 1px 4px rgba(0,0,0,0.8);
  }

  .hero a.btn {
    margin-top: 20px;
    min-width: 150px;
    font-size: 1.1rem;
    transition: transform 0.3s;
  }

  .hero a.btn:hover {
    transform: translateY(-3px);
  }
</style>

</head>
<body class="bg-light">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">🛒 Supermarket App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <% if (!user) { %>
            <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
            <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="container">
    <div class="hero mx-auto" style="max-width: 700px;">
      <% if (user) { %>
        <% if (user.role === "admin") { %>
          <h2 class="fw-bold">Welcome, <%= user.username %>!</h2>
          <p class="fs-5">Manage your <strong>inventory</strong> efficiently.</p>
          <a href="/inventory" class="btn btn-light shadow-sm">Go to Inventory</a>
        <% } else { %>
          <h2 class="fw-bold">Hello, <%= user.username %>!</h2>
          <p class="fs-5">Ready to shop for your favorite products?</p>
          <a href="/shopping" class="btn btn-light shadow-sm">Start Shopping</a>
        <% } %>
      <% } else { %>
        <h2 class="fw-bold">Welcome to Supermarket App</h2>
        <p class="fs-5">Please login or register to start using the app.</p>
        <a href="/login" class="btn btn-light shadow-sm me-2">Login</a>
        <a href="/register" class="btn btn-outline-light shadow-sm">Register</a>
      <% } %>
    </div>
  </div>

  <!-- Why Choose Us Section -->
  <div class="container py-5 bg-light rounded-4 shadow-sm mb-5">
    <h3 class="text-center fw-bold mb-4">Why Choose Us?</h3>
    <div class="row text-center">
      <div class="col-md-4 mb-3">
        <i class="bi bi-truck fs-1 text-primary"></i>
        <h6 class="mt-2">Fast Delivery</h6>
        <p class="text-muted">Get your groceries delivered quickly.</p>
      </div>
      <div class="col-md-4 mb-3">
        <i class="bi bi-egg-fried fs-1 text-success"></i>
        <h6 class="mt-2">Fresh Products</h6>
        <p class="text-muted">Only the freshest produce from trusted suppliers.</p>
      </div>
      <div class="col-md-4 mb-3">
        <i class="bi bi-cash-stack fs-1 text-warning"></i>
        <h6 class="mt-2">Best Prices</h6>
        <p class="text-muted">Competitive prices on all products.</p>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
